
spring:
  application:
    name: service-users
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        shared-configs:
          - refresh: true
            dataId: different-service.yaml
